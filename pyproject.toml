[project]
name = "doccheck"
version = "0.1.0"
description = "Structural linter and navigability metrics for large Markdown documentation trees"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
test = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["."]
